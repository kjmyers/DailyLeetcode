class Solution:
    def numberOfMatches(self, n: int) -> int:
        
        # ret = 0
        # while n > 1:
        #     cur, extra = divmod(n,2)
        #     ret += cur
        #     n = cur + extra
        
        # return ret
        return n-1
